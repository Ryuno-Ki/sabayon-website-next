machine:
  environment:
    HUGO_VERSION: 0.14
    HUGO_THEME: https://github.com/Ryuno-Ki/sabayon-website-next-theme

dependencies:
  override:
    - make dependencies

test:
  override:
    - make build

deployment:
  production:
    branch: sabayon-without-bootstrap
    commands:
      - make publish
